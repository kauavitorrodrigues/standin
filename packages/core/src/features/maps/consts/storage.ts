// Maps live under their own id so re-uploads never collide; tilesets are
// stored flat so the same tileset image can be reused across maps/entities.
export const MAPS_STORAGE_FOLDER = "maps";
export const TILESETS_STORAGE_FOLDER = "tilesets";
